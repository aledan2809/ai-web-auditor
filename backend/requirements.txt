# FastAPI & Server
# Bumped 2026-04-25 (Tier B security): 0.109.0 -> 0.128.8 covers CVE-2024-24762
# (multipart DoS), starlette 0.40+ form-parsing fixes. Tested locally on Py3.9
# (max version compatible) — VPS1 runs Py3.12.3 so could go higher but 0.128 is
# the LTS-style line with all CVEs of 0.109..0.128 lineage applied.
fastapi==0.128.8
uvicorn[standard]==0.32.1
python-multipart==0.0.20

# Database
sqlalchemy==2.0.25
aiosqlite==0.19.0
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.1

# Authentication
python-jose[cryptography]==3.3.0
bcrypt==4.0.1

# Payments
stripe==7.0.0

# HTTP & Scraping
httpx==0.26.0
beautifulsoup4==4.12.3
lxml==5.1.0
playwright==1.41.0

# AI
anthropic==0.18.0

# PDF Generation
reportlab==4.0.9
weasyprint==60.2

# Validation & Utils
# Bumped 2026-04-25 (Tier B): pydantic 2.5.3 -> 2.10.4, pydantic-settings 2.1.0 -> 2.6.1
# (compatible with fastapi 0.128.x; tested via pip resolver locally).
pydantic[email]==2.10.4
pydantic-settings==2.6.1
python-dotenv==1.0.0
email-validator==2.1.0

# Security Scanning
requests==2.31.0
urllib3==2.1.0

# Image Processing (screenshots)
pillow==10.2.0

# Async
aiofiles==23.2.1

# Cloud Storage (S3/R2/Spaces)
boto3==1.34.0

# Email
sendgrid==6.11.0

# Date/Time
python-dateutil==2.8.2
